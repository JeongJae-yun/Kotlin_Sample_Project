<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kotlin_sample">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/kotlin_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/kotlin_icon_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="stateAlwaysHidden"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".ImageLoadSample.ImageLoadActivity"></activity>
        <activity android:name=".listSample.RecyclerViewActivity"></activity>
        <activity android:name=".listSample.MainRecycler" />
        <activity
            android:name=".view.main.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>